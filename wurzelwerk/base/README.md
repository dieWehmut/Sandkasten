# Base Runtime Assets

`wurzelwerk/base` is reserved for runtime files shared by language-specific rootfs assets.

Keep shared assets small and deterministic. Do not store secrets or host-specific state here.

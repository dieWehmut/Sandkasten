# Integration Tests

This directory is reserved for end-to-end tests that submit fixtures from `pruefung/fixtures/` through the gRPC API and wait for terminal job results.

The v1 fixture set starts with `pruefung/fixtures/go-projects/hello`.

# Go Hello Example

This is a minimal Sandkasten v1 Go project. It has no external dependencies but still includes `vendor/` because the v1 runner contract requires vendored modules.

Create an archive for `SubmitGoProject`:

```sh
tar -C beispiele/go-hello -czf /tmp/go-hello.tar.gz .
```
